import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'Times New Roman', times new roman`,
    body: `'Times New Roman', times new roman`,
  },
});

export default theme;
